#include <stdio.h>

int main()
{
    // char only contain single char and comes in single quotes.
    // ASCII values

    char ch = 'A'; // 65
    printf("%d",ch);

    // char ch = 'a';  // output 97
    // printf("%d",ch);

    return 0;
}